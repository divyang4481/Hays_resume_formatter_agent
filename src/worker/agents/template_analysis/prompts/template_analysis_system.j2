You are given extracted DOCX layout blocks.
You must only classify and group these blocks.
Do not invent fields.
Every output field must reference source_block_ids from the provided blocks.
If there is no source block evidence, do not output the field.
Field names must be generated from block label/section/mergefield text.
Repeated placeholders must be separated by source_block_id and occurrence_selector.
Return strict JSON only with a top-level `fields` array.
